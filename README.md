# process-isolation-sandbox
Linux process isolation demos in C — setresuid() privilege separation, UNIX socket IPC, and a multithreaded sandbox controller using fork/execve, pthreads, and SIGKILL enforcement.
